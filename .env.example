# ClickUp API configuration
#
# The server loads secrets from this file first (if provided via --env or auto-discovered),
# then applies command-line overrides. Preferred variable is CLICKUP_API_TOKEN.
# Do NOT commit real tokens.

# Preferred API token used by the server and examples
CLICKUP_API_TOKEN=

# Fallback token mainly used by examples/tests. If CLICKUP_API_TOKEN is empty, the server
# will also look for this variable.
E2E_TEST_API_TOKEN=

# Optional: IDs used by E2E tests (set to real IDs from your workspace)
# Team/workspace to create temporary resources in E2E tests
CLICKUP_TEST_TEAM_ID=
# Space used by folder/list tests (if not creating dynamically)
CLICKUP_TEST_SPACE_ID=
# Folder used by list tests (if not creating dynamically)
CLICKUP_TEST_FOLDER_ID=
# Primary list for task tests
CLICKUP_TEST_LIST_ID=
# Secondary list for TIML tests (tasks in multiple lists)
CLICKUP_TEST_LIST_ID_2=
# Custom field ID used in custom field tests
CLICKUP_TEST_CUSTOM_FIELD_ID=


# Webhook consumer and handlers (optional)
#
# Declare user handler modules (comma-separated) that should be auto-imported by the
# web server and the webhook consumer at startup. Example:
# CLICKUP_WEBHOOK_HANDLER_MODULES=my_project.webhooks.handlers,my_project.more.handlers
CLICKUP_WEBHOOK_HANDLER_MODULES=

# Message queue backend for the webhook event sink/consumer. The available backend
# names depend on your abe backends setup. Common value for local development:
#   QUEUE_BACKEND=local
# If unset, defaults may apply in the consumer CLI.
QUEUE_BACKEND=


# ──────────────────────────────────────────────────────────────────────────────
# Middleware Configuration (CORS)
# ──────────────────────────────────────────────────────────────────────────────
# Allowed origins for Cross-Origin Resource Sharing (CORS).
# Expects a JSON-formatted list of strings. Default is ["*"].
CORS_ALLOW_ORIGINS=["*"]

# Indicate that cookies should be supported for cross-origin requests.
# Default is True.
CORS_ALLOW_CREDENTIALS=True

# List of allowed HTTP methods. Expects a JSON-formatted list of strings.
# Default is ["*"].
CORS_ALLOW_METHODS=["*"]

# List of allowed HTTP headers. Expects a JSON-formatted list of strings.
# Default is ["*"].
CORS_ALLOW_HEADERS=["*"]


# ──────────────────────────────────────────────────────────────────────────────
# Additional notes
# ──────────────────────────────────────────────────────────────────────────────
# • Auto-discovery: If you don't pass --env, the server will attempt to locate
#   a .env file from the current directory upwards (parent directories).
# • CLI precedence: A value provided by --token will override any value set here.
# • Encoding: Keep this file in UTF-8; lines are KEY=VALUE with no quotes needed
#   unless your value contains spaces or special characters.
# • Importing handler modules: Any module path listed in
#   CLICKUP_WEBHOOK_HANDLER_MODULES must be importable on PYTHONPATH when the
#   server/consumer starts (e.g., installed in the environment or on sys.path).
# • Queue backend: The webhook producer/consumer uses QUEUE_BACKEND to select a
#   message queue implementation. For local development, "local" is typically
#   sufficient. Other values depend on your abe backends installation.

# Minimal example
# CLICKUP_API_TOKEN=sk_clickup_XXXXXXXXXXXXXXXX
# CLICKUP_WEBHOOK_HANDLER_MODULES=my_app.webhooks.handlers
# QUEUE_BACKEND=local

# E2E IDs guidance (optional)
# • CLICKUP_TEST_TEAM_ID: Team/Workspace ID for creating resources during tests
# • CLICKUP_TEST_SPACE_ID: Space ID used by some folder/list tests
# • CLICKUP_TEST_FOLDER_ID: Folder ID for list tests (when not creating dynamically)
# • CLICKUP_TEST_LIST_ID: Primary list for task tests
# • CLICKUP_TEST_LIST_ID_2: Secondary list for tests involving moves or multiple lists
# • CLICKUP_TEST_CUSTOM_FIELD_ID: Custom field ID used in custom field scenarios
